[project]
name = "plush-dev"
version = "0.0.0"
requires-python = ">= 3.12"
dependencies = [
    "plush-fabric[test,publish]",
]

[tool.uv.sources]
plush-fabric = { path = "./python", editable = true }
